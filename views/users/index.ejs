<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <% users.forEach((user) => { %>
    <p>
        <ul>
            <li><a href="/users/<%= user._id %>/users/<%= user._id %>"><%= user.username %></a></li>
        </ul>
    </p>
   <% }) %>
</body>
</html>